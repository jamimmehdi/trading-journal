// This is a template. The real js/config.js is generated automatically:
// - Locally: copy this file to js/config.js and fill in your own values (that file is gitignored).
// - On GitHub Pages: the deploy workflow (.github/workflows/deploy.yml) generates it
//   from the SUPABASE_URL and SUPABASE_ANON_KEY repository secrets.
window.SUPABASE_CONFIG = {
  url: "__SUPABASE_URL__",
  anonKey: "__SUPABASE_ANON_KEY__",
};
